# OS / 에디터
.DS_Store
Thumbs.db
.vscode/
.idea/

# 혹시라도 실수로 만들어질 수 있는 로컬 설정/키 파일
*.local
.env
.env.*
secrets*

# 확장프로그램 빌드 산출물 (있다면)
*.zip
*.crx
*.pem

# 단건조회 응답 샘플 (필드 위치 확인용, 커밋 불필요)
scripts/_sample_query_product.json
scripts/_sample_seller_product.json

# 쓰기 정찰 응답 덤프 (필드 구조 확인용, 커밋 불필요)
scripts/_probe_*.json

# npm 설치 산출물 — 커밋하지 않는다 (VPS/로컬에서 각자 npm install)
node_modules/
