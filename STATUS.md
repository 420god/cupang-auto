# 지금 상태

> **언제 읽나**: 세션 시작할 때 항상. 마이그레이션·배포 상태를 확인할 때.
> **최종 갱신**: 2026-08-22
> **성격**: 자주 바뀌는 **사실**만. "왜"는 `docs/`에, 규칙은 `docs/rules/INDEX.md`에.

## 마이그레이션

| 번호 | 내용 | 실행 |
|---|---|---|
| 001~017 | 기존 + 상품원장·공급·소싱 스키마 | ✅ 확인됨 |
| 018 | registry에 item_name | ✅ 확인됨 |
| 019 | 작업비·불량·로트분할·원가 2축 | ✅ 사용자 실행 |
| 020 | 로트 실제 도착 수량(qty_arrived) | ✅ 확인됨 |
| 021 | inventory_lots.sku_id nullable | ✅ 사용자 실행 |
| 022 | 로트 취소 + 예치금 기록 확장 | ✅ 확인됨 |
| 023 | 쿠팡 쓰기 큐(가격 변경) | ✅ 확인됨 (가격 변경·등록 큐가 실제로 돈다) |
| 024 | 판매가·판매여부·재고 + 가격 변동 이력 | ✅ 확인됨 |
| 025 | 상품 정보 수정 큐 + 이미지 버킷 | ✅ 확인됨 |
| 026 | 변경 이력 + 일별 옵션 지표(비즈니스 인사이트) | ✅ 확인됨 |
| 027 | 지표를 실제 API 기준으로 + 유입경로 + 동기화 로그 | ✅ 확인됨 |
| 028 | v_product_experiments 뷰 (변경 × 전후 지표) | ✅ 확인됨 |
| 029 | 신규 상품 등록(복제) + 소싱 판단 연결 + 카테고리 메타 | ✅ 확인됨 |
| 030 | 상품 등록 초안 | ⬜ 미실행 — **안 돌려도 된다.** 031 파이프라인이 그 역할을 대신한다(D-21) |
| 031 | 등록 파이프라인 (준비 건·옵션·이미지 후보·단계별 근거·뼈대 템플릿) | ✅ 확인됨 (추가 컬럼까지) |
| 032 | 카탈로그 매칭 결과(제조사·카탈로그ID·원문) | ✅ 확인됨 |
| 033 | 상세페이지 세트(set_no·set_label) | ✅ 확인됨 |
| 034 | skuInfo 날짜 플래그 2개(제조일·생산일) | ✅ 확인됨 |
| 035 | 뼈대 두 종류(배송 / 고시·주요정보) + 진행 판정 갱신 | ✅ 사용자 실행 |
| 036 | 뼈대 완료 판정에서 복제 원본 예외 제거 | ✅ 확인됨 |
| 037 | 등록 설정(늘 같은 값) + 준비 건 검색필터 | ✅ 확인됨 |
| 038 | 옵션마다 필수속성 값(attributes) | ✅ 확인됨 |

**실행 여부를 확인하는 법** — Supabase REST에 물어보면 된다. RLS 때문에 내용은 안 보이지만 **존재 여부는 응답 코드로 갈린다**: 없으면 `PGRST205` 404, 있으면 200 + 빈 배열.

```bash
curl -s -o /dev/null -w '%{http_code}\n' \
  'https://winpzjbisxxsstmynywp.supabase.co/rest/v1/<테이블>?select=<컬럼>&limit=1' \
  -H 'apikey: <publishable key>' -H 'Authorization: Bearer <publishable key>'
```

확실히 있는 테이블(`rocket_growth_sales_daily`)을 대조군으로 같이 조회하면 오탐을 막는다.

## 배포

| 대상 | 방법 | 주의 |
|---|---|---|
| 웹 | `main` push → Vercel 자동배포 | 웹훅이 가끔 유실됨 → 배포 목록에 그 커밋이 없으면 `git commit --allow-empty` 후 push |
| VPS 스크립트 | VPS에서 `git pull` | push만으론 반영 안 됨. 경로 `/home/thezone1633/cupang-auto` |
| VPS 쓰기 워커 | `git pull && **sudo** systemctl restart coupang-write-worker` — **sudo 없이 하면 root 비밀번호를 묻는다** | 상시 프로세스라 pull만으론 옛 코드가 계속 돈다. **안 떠 있으면 웹의 가격 변경이 큐에 쌓이기만 한다** (설치법은 `scripts/coupang-write-worker.service` 주석) |
| 확장프로그램 | `chrome://extensions`에서 새로고침 | git과 무관 |

## 웹 코드 구조 (2026-08-20 분할)

`web/app.js` 4,971줄 하나 → `web/js/00-core.js` … `95-boot.js`. 등록 파이프라인 화면들이 86~94를 쓴다.
**모듈이 아니라 순서 있는 일반 `<script>`다** — 파일명 앞 숫자가 로드 순서고, 바꾸면
조용히 깨진다(D-17). `web/api/`는 청구서 파서 하나만 남았다(고정 IP 우회 잔재 삭제).

## 화면 (21개)

```
소싱   소싱 · 카테고리 · 수집 대기열 · 즐겨찾기
등록   상품등록 · ①카테고리 · ②상품명·검색어 · ③옵션·가격 · ④검색필터
       · ⑤대표이미지 · ⑥상세페이지 · ⑦물류·바코드 · 등록 설정
       **카테고리를 먼저 정해야 나머지 화면의 입력이 풀린다**(2026-08-21)
       카탈로그 매칭은 카테고리 화면 안으로 들어갔다(별도 화면 없음)
상품   상품원장 · 상품수정
공급   발주 · 입고 · 재고 · 출고
경영   예치금
판매   판매현황
```

## 등록 파이프라인 (2026-08-22 완성 — 첫 등록만 남음)

소싱 즐겨찾기 → [등록 준비] → 준비 건(`listing_projects`)에 단계별로 값을 쌓고 →
상품등록 화면에서 다 찼는지 보고 쏜다. 설계 이유는 D-21, 상세는
`docs/domain/product-listing.md`.

| 단계 | 화면 | 상태 |
|---|---|---|
| 목록·진행 판정 | 상품등록 | ✅ |
| 옵션·필수속성·판매가·추정 원가 | 옵션·가격 | ✅ 옵션명 자동 생성 (038 실행 필요) |
| 상품명·노출명·브랜드·검색어 | 상품명·검색어 | ✅ |
| 카테고리(검색·코드 입력) + 필수속성 캐시 | 카테고리 | ✅ |
| 카탈로그 매칭(브랜드·제조사·카테고리·조회수·판매량) | **카테고리 화면 안** | ✅ (확장 새로고침 필요) |
| 뼈대(배송·반품지·과세·고시정보) 양식 | **등록 설정** 안 | ✅ |
| 검색필터(카테고리 선택 속성 25개, 드롭다운·단위) | 검색필터 | ✅ |
| 등록 설정 기본값(브랜드·제조사·고시 기본값·크기등급·출고일) | 등록 설정 | ✅ |
| 양식 빈 것 만들기·복사·이름변경·삭제 | 등록 설정 | ✅ |
| 대표이미지(후보 보관·교체 이력) | 대표이미지 | ✅ |
| 상세페이지(세트 단위·긴 이미지) | 상세페이지 | ✅ |
| 물류·바코드(skuInfo 22항목·바코드 방식) | 물류·바코드 | ✅ |
| 등록 실행(payload 빌드 → 확인 → 큐) | 상품등록 | ✅ (워커 2026-08-21 12:12 UTC 재시작 완료) |

**2026-08-22 회귀 한 건을 고쳤다** — 커밋 `8800c16`(038 옵션명)이 `lstBuildAttributes`로
갈아끼우면서 **그 아래 178줄을 같이 지웠다**: `lstOpenSubmit` · 모달 닫기 · `#lstSubGo` ·
`lstWatchQueue`. 모달 마크업은 `index.html`에 남아 있어 겉으론 멀쩡했고, 등록 버튼을 누르면
ReferenceError로 조용히 죽어 아무 반응이 없었다(상태는 계속 `준비중`). `8800c16^`에서
그대로 복원했다.

**031~038 전부 실행됐다.** 화면 10개를 각각 실제 DB로 왕복시켜 확인했다(S-2026-08-22).
**2026-08-22 첫 등록에 성공했다.** WING에서 값을 대조했고 **제조사 한 칸만 틀렸다** —
카탈로그 매칭이 넣은 경쟁 상품 제조사("루모아")가 그대로 나갔다. 고쳤다:
카탈로그는 브랜드·제조사를 더는 안 쓰고, 제조사 칸을 상품명·검색어 화면에 만들었다
(`docs/domain/product-listing.md` 「상품의 신원은 카탈로그에서 오지 않는다」).

**순서를 화면이 강제한다**(D-22): 카테고리 → 옵션 → 나머지. 못 채우면 입력이 잠기고
무엇이 없어서 잠겼는지 배너가 말한다.

**원가 두 종류를 섞지 말 것** — `listing_project_items`의 단가·개당원가는 **소싱 추정치**이고,
확정 원가는 `inventory_lots`(청구서 기준)에 있다. 확정이 나와도 추정을 덮지 않는다(R-05).
덮으면 "추정이 얼마나 틀렸나"를 못 본다.

## 미해결

**공급망**
- 밀크런 택배비(쿠팡이 화주에 직접 청구)를 아직 시스템에 안 넣음 — 확인 경로는 WING
- 예치금 잔액 대사(계산 잔액 vs 대행사 통보 잔액) 차이를 자동으로 짚어주지 않음
- 한국 도착 후 발견한 불량을 처리할 "재고 조정" 기능 없음

**판매현황**
- `item_calc` 비어 있어 마진율 정렬·필터 안 먹음
- 상품/옵션별 상세표 수수료+입출고비는 확정 정산과 **구조적으로 100% 못 맞음**(반품처리비 미귀속) — 이미 막다른 길로 결론남, 다시 시도하지 말 것 → `docs/decisions/INDEX.md` D-08
- 카테고리 수수료 매칭 커버리지 부족 → 매칭 안 된 상품은 이익 추정이 0으로 깔림
- 개당 쿠폰비가 WING "최종구매가" 기준과 일치하는지 미검증

**쿠팡 쓰기 · 등록**
- ~~첫 등록을 아직 안 해봤다~~ → **2026-08-22 첫 등록 성공.** WING 대조도 했다
- **이미 등록된 첫 상품의 제조사가 아직 "루모아"다** — 코드는 고쳤지만 등록된 값은 그대로다.
  WING에서 직접 고치거나, `product_update` 큐로 `payload.product.manufacture`만 쏘면 된다
  (화면에 그 입구는 아직 없다)
- 첫 등록 상품의 **판매기간이 "설정함 · ~2099-12-31"** 로 들어갔다 — 뼈대의 `saleEndedAt`이
  따라온 것. 사실상 무기한이라 해는 없지만 의도한 값인지 미확인
- 미검증 **6가지**(자체 바코드 · 면세 · 성인전용 · 병행수입 · 혼합구성 · 유통기한 필수 카테고리).
  **실물이 없어서** 못 푼다 — 코드로 해결되는 게 아니다. 2026-08-22 결정: **막지 않고**
  등록 직전 확인 모달이 노란 박스로 "처음 가는 길"이라고 말한다. 목록은
  `web/js/86-listing.js` 의 `LISTING_UNVERIFIED` 한 곳 — 대조가 끝나면 그 줄을 지운다
  (전에는 화면마다 따로 적어서 병행수입이 한쪽은 검증됨, 한쪽은 미검증이었다)
- `producedAtManaged` vs `manufacturedAtManaged` — WING의 "제조일"이 어느 쪽인지 모름(13건 전부 false)
- 판매상태(중지/재개) 미검증 — 워커가 거부 중
- 빈 양식 등록(복제 원본 없이) 미지원 — 워커가 거부한다
- 소싱 화면이 미완성이라 옵션·가격이 아직 소싱에서 안 넘어온다(`price_source='manual'`만 생김)
- ~~상품원장의 공급처 이관~~ → **2026-08-22 완료.** 등록 준비(옵션·가격)에서만 넣고,
  `rocket-growth-sync.js` 가 SKU를 만들 때 `sku_suppliers` 로 옮긴다. **VPS `git pull` 필요**
- **구매요청(발주요청) 화면이 없다** — 지금 발주는 청구서부터 시작한다. 그 앞의 구매요청은
  쿠플러스 웹에서 사람이 치고 **어디에도 안 남는다.** 한글표시사항 8필드가 여기서 쓰인다
  (그래서 상품원장에 그대로 둔 것). 요청을 표로 남겨 청구서와 잇기로 결정(2026-08-22, 미착수)

**기타**
- SKU ID(WING 화면 8자리)는 공식 Open API 어디에도 없음
- `fulfillment_fees`에 전용할인가 행이 없는 카테고리는 요금표 강제 재수집 필요
